# Regras de UX/UI
- **NUNCA** utilizar caixas de diálogo nativas do navegador (como `alert()`, `prompt()` ou `confirm()`). Todas as notificações, mensagens de confirmação e solicitações de entrada de dados devem ser feitas usando modais customizados dentro do sistema (UI In-App).
